spring.application.name=backend

# ===============================
# = SERVER CONFIGURATION =
# ===============================
server.port=8080

# ===============================
# = DATABASE CONFIGURATION =
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/paf_148?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# ===============================
# = WEB SOCKET CONFIG =
# ===============================
spring.websocket.enabled=true

# ===============================
# = CORS (if front-end on different port) =
# ===============================
# Allow requests from frontend React dev server
# (adjust origin if needed)
spring.web.cors.allowed-origins=http://localhost:3000


# File upload settings
upload.directory=./uploads
server.url=http://localhost:8080

# Spring multipart settings
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# ===============================
# = LOGGING =
# ===============================
logging.level.org.springframework=INFO
